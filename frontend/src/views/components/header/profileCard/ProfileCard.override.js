import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
    profileActionsBtn: {
        height: '50%',
        alignSelf: 'center',
        boxShadow: '0 0 0 2.5px rgb(68, 55, 55), 1px 1px 0 3px rgba(159, 158, 158, 0.644)',
    },
});

export default useStyles;
