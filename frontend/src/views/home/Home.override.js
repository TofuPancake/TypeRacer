import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
    label: {
        marginTop: '10px',
        marginBottom: '10px',
        minWidth: '300px',
        backgroundColor: '#dbf6e9d0',
        borderRadius: '10px',
    },
    homeButton: {
        fontSize: 'xx-large',
        width: '100%',
        minHeight: '75px',
    }
});

export default useStyles;